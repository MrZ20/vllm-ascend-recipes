# Single-node deployment-verify whitelist.
# One extended-regex pattern per line, matched against the changed recipe path
# (e.g. `models/en/Qwen/` whitelists every Qwen recipe).
# pr-recipe-verify.yml reads this file; recipes not matching any pattern are
# skipped on pull_request triggers (manual workflow_dispatch always runs the
# recipe selected by the caller).
models/en/Qwen/
